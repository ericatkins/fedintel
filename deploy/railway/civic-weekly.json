{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "sh -c 'python -m src.enrich --refresh-legislators && python -m src.enrich --refresh-legislator-funding && python -m src.enrich --refresh-district-spending && python -m src.enrich --refresh-candidate-finance'",
    "cronSchedule": "0 7 * * 0",
    "restartPolicyType": "NEVER"
  }
}
