{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "sh -c 'python -m src.enrich --refresh-forecasts && python -m src.enrich --refresh-grants && python -m src.enrich --link-oversight'",
    "cronSchedule": "0 9 * * 1",
    "restartPolicyType": "NEVER"
  }
}
